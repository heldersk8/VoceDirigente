using System.Collections.Generic;
using NHibernate;
using RecrutaZero.Dominio;
using RecrutaZero.Infra._Base.Repositorios;

namespace RecrutaZero.Infra.Repositorios.Dominio
{
    //public class PassoRepositorio : RepositorioBaseDominio<Passo>, IPassoRepositorio
    //{
    //    public PassoRepositorio(ISession sessao) : base(sessao) { }

    //    public IEnumerable<Passo> ObterPor(Ocupacao ocupacao)
    //    {
    //        return Entidades().Where(x => x.Ocupacao == ocupacao).ToList();
    //    }
    //}
}